{
  "name": "order-of-86-website",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm ci --prefix play-app && npm run build --prefix play-app"
  },
  "engines": {
    "node": ">=22.13 <23"
  },
  "dependencies": {
    "canvas": "^3.2.1",
    "gif-encoder-2": "^1.0.5",
    "omggif": "^1.0.10"
  }
}
