import {gameRewards} from './minigame-rewards-core.js';
export function beginMinigame(kind,id=crypto.randomUUID()){globalThis.dispatchEvent(new CustomEvent('paw-minigame-xp',{detail:{operation:'minigame-start',kind,run:id}}));return id;}
export function finishMinigame(kind,run,won=false){if(run)globalThis.dispatchEvent(new CustomEvent('paw-minigame-xp',{detail:{operation:'minigame-finish',kind,run,completed:true,won}}));}
export function createMinigameRewards(getShop,notify){globalThis.addEventListener('paw-minigame-xp',e=>{const b=e.detail;if(b.operation==='minigame-start')document.querySelectorAll('.minigame-xp-note').forEach(n=>n.remove());void getShop().run(b).then(ok=>{if(ok&&b.operation==='minigame-finish'){const xp=gameRewards[b.kind].xp+(b.kind==='lava'&&b.won?100:b.kind==='chess'&&b.won?50:0);const text='Minigame complete · +'+xp+' Order XP';notify(text);const dialog=document.querySelector('#lavaLobby[open],#boatRaceDialog[open],#sledRaceDialog[open],#sunMaze[open],#chessDialog[open]');if(dialog){const note=document.createElement('p');note.className='minigame-xp-note';note.setAttribute('role','status');note.textContent=text+' · Saved';dialog.append(note);}}else if(!ok&&b.operation==='minigame-finish')notify('Minigame XP could not be saved: '+document.getElementById('shopError').textContent);});});}
