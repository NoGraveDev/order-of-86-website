import {beginMinigame,finishMinigame} from './minigame-rewards.js';
import {recordMinigame} from './minigame-records.js';
import * as T from 'three';
import {height} from './geography.js';
import {sunwardSites} from './sunward-layout.js';
import {makeMaze,moveMaze,mazeViewport} from './sunward-maze-model.js';
export function createSunwardMaze({world,player,onStart,onEnd,blocked=()=>false}){
 const site=sunwardSites.find(p=>p.id==='sun-labyrinth');
 // A small golden maze tablet marks the entrance without blocking the road.
 const marker=new T.Group();marker.name='Sunward_Labyrinth_Tablet';marker.position.set(site.x,height(site.x,site.z),site.z);world.add(marker);
 const stone=new T.MeshStandardMaterial({color:'#d7a52d',roughness:.85}),light=new T.MeshBasicMaterial({color:'#fff2a0'});
 const base=new T.Mesh(new T.CylinderGeometry(2.5,3,.5,8),stone);base.position.y=.25;marker.add(base);
 for(let i=0;i<7;i++){const wall=new T.Mesh(new T.BoxGeometry(i%2?2:3.5,.2,.18),light);wall.position.set(i%2?.5:0,.6,-1.5+i*.5);marker.add(wall);}
 const beacon=new T.Mesh(new T.OctahedronGeometry(.5),light);beacon.position.y=3.2;marker.add(beacon);
 const style=document.createElement('style');style.textContent=`
 #sunMaze{box-sizing:border-box;width:min(960px,98vw);max-width:98vw;max-height:98dvh;padding:20px;background:#201f18;color:#fff0c4;border:1px solid #c5a457;border-radius:20px;overflow:auto}
 #sunMaze::backdrop{background:#10160fea;backdrop-filter:blur(8px)}
 #sunMaze *{box-sizing:border-box}#sunMaze h2{margin:3px 0 10px;font-size:28px;color:#fff0bc}#sunMaze p{margin:8px 0;line-height:1.45}
 #sunMaze .maze-kicker{font:11px monospace;letter-spacing:2px;color:#dfba69}#sunMaze .maze-layout{display:grid;grid-template-columns:minmax(0,1fr) 210px;gap:20px;align-items:center}
 #sunMaze .maze-board{display:block;width:100%;max-height:calc(98dvh - 135px);aspect-ratio:1;object-fit:contain;border-radius:12px;touch-action:none}
 #sunMaze [hidden]{display:none!important}
 #sunMaze .maze-view-controls{display:flex;gap:6px;flex-wrap:wrap;margin:8px 0}#sunMaze .maze-view-controls button{flex:1;min-width:55px;font-size:13px}#sunMaze .maze-overview{display:block;width:150px;height:150px;max-width:100%;border:1px solid #b99a52;border-radius:8px;margin:8px auto;image-rendering:pixelated}#sunMaze .maze-map-note{font-size:12px;color:#d8c79e}
 #sunMaze button,#sunMaze select{min-height:44px;background:#39382a;color:#fff0c4;border:1px solid #8b7846;border-radius:9px;padding:9px;cursor:pointer;font:inherit}#sunMaze button:focus-visible,#sunMaze select:focus-visible{outline:3px solid #ffe09a;outline-offset:2px}
 #sunMaze .maze-pad{display:grid;grid-template-columns:repeat(3,48px);gap:5px;justify-content:center;margin:16px 0;touch-action:none}#sunMaze .maze-pad button{font-size:23px;user-select:none;touch-action:none}
 #sunMaze .maze-actions{display:flex;gap:8px;flex-wrap:wrap}#sunMaze .maze-stats{font:15px monospace;color:#ffe09a;min-height:22px}#sunMaze .maze-result{color:#cbeaac;min-height:45px}#sunMaze select{width:100%}
 #mazeEnter{position:fixed;bottom:230px;left:50%;transform:translateX(-50%);z-index:14;background:#695020;color:#fff0c4;max-width:90vw}
 @media(max-width:600px) and (orientation:portrait){#sunMaze{padding:12px}#sunMaze h2{font-size:23px}#sunMaze .maze-layout{grid-template-columns:1fr;gap:8px}#sunMaze .maze-board{max-height:46dvh}#sunMaze .maze-side{display:grid;grid-template-columns:minmax(0,1fr) 142px;gap:8px;align-items:center}#sunMaze .maze-pad{margin:0;grid-template-columns:repeat(3,44px)}#sunMaze select{font-size:12px;min-width:0;padding:6px}#sunMaze .maze-help{font-size:12px}#sunMaze .maze-result{min-height:0}#sunMaze .maze-actions{grid-column:1/-1}#sunMaze .maze-stats{font-size:12px}}
 @media(orientation:landscape) and (max-height:550px){#sunMaze{padding:10px}#sunMaze h2{font-size:20px;margin:0}#sunMaze .maze-layout{grid-template-columns:minmax(0,1fr) 280px;gap:10px}#sunMaze .maze-board{max-height:calc(98dvh - 110px)}#sunMaze .maze-side{font-size:12px}#sunMaze .maze-pad{margin:5px}#sunMaze .maze-pad button{min-height:38px;padding:3px}#sunMaze p{margin:3px 0}}
 #sunMaze.maze-large .maze-help{display:none!important}#sunMaze.maze-large .maze-overview{width:112px;height:112px}#sunMaze.maze-large .maze-result{min-height:0}#sunMaze.maze-large .maze-pad{margin:8px 0}#sunMaze.maze-large .maze-layout{align-items:start}#sunMaze.maze-large .maze-board{max-height:calc(90dvh - 150px)}#sunMaze.maze-large .maze-map-note{font-size:11px!important;line-height:1.3!important}
 @media(max-width:600px) and (orientation:portrait){#sunMaze.maze-large .maze-side{align-items:start}#sunMaze.maze-large .maze-overview{width:96px;height:96px}#sunMaze.maze-large .maze-map-note{display:none!important}#sunMaze.maze-large .maze-board{max-height:38dvh}}
 `;document.head.append(style);
 const dialog=document.createElement('dialog');dialog.id='sunMaze';dialog.setAttribute('aria-labelledby','mazeTitle');dialog.innerHTML=`<div class="maze-kicker">SUNWARD HEIGHTS · A TRIAL OF CLARITY</div><h2 id="mazeTitle">The Golden Labyrinth</h2><div class="maze-layout"><canvas class="maze-board" tabindex="0" width="760" height="760" aria-label="Overhead labyrinth. Guide the wizard dog from the upper left to the glowing exit at the lower right."></canvas><div class="maze-side"><div><select aria-label="Maze difficulty"><option value="9">Wanderer · 9 × 9</option><option value="13">Pathfinder · 13 × 13</option><option value="86">The Order’s Trial · 86 × 86</option></select><p class="maze-help">Guide the wizard dog to the golden exit. WASD / arrows, or hold the direction buttons. The 86 × 86 trial follows your wizard; zoom for a clearer path.</p><div class="maze-view-controls" hidden><button data-action="zoom-in" aria-label="Zoom in on maze">＋</button><button data-action="zoom-out" aria-label="Zoom out of maze">−</button><button data-action="overview">Full map</button></div><canvas class="maze-overview" width="192" height="192" aria-label="Whole maze overview with your position and exit" hidden></canvas><p class="maze-map-note" hidden>Gold: exit · violet: your wizard. Movement returns to follow view.</p><p class="maze-stats"></p><p class="maze-result" role="status" aria-live="polite"></p></div><div class="maze-pad"><span></span><button data-dir="0,-1" aria-label="Move up">↑</button><span></span><button data-dir="-1,0" aria-label="Move left">←</button><button data-dir="0,1" aria-label="Move down">↓</button><button data-dir="1,0" aria-label="Move right">→</button></div><div class="maze-actions"><button data-action="retry">Retry</button><button data-action="new">New maze</button><button data-action="exit">Leave maze</button></div></div></div>`;document.body.append(dialog);
 const enter=document.createElement('button');enter.id='mazeEnter';enter.hidden=true;enter.textContent='Golden Labyrinth · E';document.body.append(enter);
 const canvas=dialog.querySelector('canvas'),ctx=canvas.getContext('2d'),stats=dialog.querySelector('.maze-stats'),result=dialog.querySelector('.maze-result'),select=dialog.querySelector('select');
 let rewardRun=null;
 const backdrop=document.createElement('canvas'),backdropCtx=backdrop.getContext('2d'),overview=dialog.querySelector('.maze-overview'),overviewCtx=overview.getContext('2d'),zoomLevels=[11,17,23,35,51];let zoom=2,fullMap=false,dirty=true,lastStats=0,staticBuilds=0;
 let state=makeMaze(),elapsed=0,started=false,held=null,nextStep=0,lastTime=0,frame=0,lastFocus=null;
 const dirs={ArrowUp:[0,-1],KeyW:[0,-1],ArrowDown:[0,1],KeyS:[0,1],ArrowLeft:[-1,0],KeyA:[-1,0],ArrowRight:[1,0],KeyD:[1,0]};
 function buildBackdrop(){
  const tile=8,n=state.size;backdrop.width=backdrop.height=n*tile;backdropCtx.fillStyle='#e9dbad';backdropCtx.fillRect(0,0,backdrop.width,backdrop.height);
  for(let y=0;y<n;y++)for(let x=0;x<n;x++)if(state.grid[y][x]){backdropCtx.fillStyle='#96712d';backdropCtx.fillRect(x*tile,y*tile,tile,tile);backdropCtx.fillStyle='#caa952';backdropCtx.fillRect(x*tile+1,y*tile+1,tile-2,tile-2);}
  backdropCtx.fillStyle='#adc298';backdropCtx.fillRect(tile,tile,tile,tile);backdropCtx.fillStyle='#ebae35';backdropCtx.fillRect(state.exit.x*tile,state.exit.y*tile,tile,tile);staticBuilds++;dirty=true;
 }
 function draw(){
  if(!dirty)return;dirty=false;const n=state.size,view=state.cells===86&&!fullMap?mazeViewport(state,zoomLevels[zoom]):{x:0,y:0,span:n},s=720/view.span,ox=20-view.x*s,oy=20-view.y*s;
  ctx.fillStyle='#191f19';ctx.fillRect(0,0,760,760);ctx.imageSmoothingEnabled=false;ctx.drawImage(backdrop,view.x*8,view.y*8,view.span*8,view.span*8,20,20,720,720);
  if(state.cells===86){overviewCtx.imageSmoothingEnabled=false;overviewCtx.drawImage(backdrop,0,0,192,192);const scale=192/n;overviewCtx.strokeStyle='#fff9d6';overviewCtx.lineWidth=2;overviewCtx.strokeRect(view.x*scale,view.y*scale,view.span*scale,view.span*scale);overviewCtx.fillStyle='#8d35dc';overviewCtx.beginPath();overviewCtx.arc((state.x+.5)*scale,(state.y+.5)*scale,3,0,Math.PI*2);overviewCtx.fill();overviewCtx.fillStyle='#ffb300';overviewCtx.fillRect(state.exit.x*scale-2,state.exit.y*scale-2,5,5);}
  canvas.setAttribute('aria-label','Labyrinth '+state.cells+' by '+state.cells+'. Wizard at column '+state.x+', row '+state.y+'. '+(fullMap?'Full map overview.':'Follow view.'));
  if(fullMap&&state.cells===86){ctx.fillStyle='#7830c4';ctx.beginPath();ctx.arc(ox+(state.x+.5)*s,oy+(state.y+.5)*s,6,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#ffffff';ctx.lineWidth=2;ctx.stroke();}
  // Overhead storybook dog: ears, muzzle, paws, and a pointed violet wizard hat.
  ctx.save();ctx.translate(ox+(state.x+.5)*s,oy+(state.y+.5)*s);ctx.scale(s/40,s/40);ctx.strokeStyle='#29232e';ctx.lineWidth=1.5;
  function oval(x,y,rx,ry,color){ctx.beginPath();ctx.ellipse(x,y,rx,ry,0,0,Math.PI*2);ctx.fillStyle=color;ctx.fill();ctx.stroke();}
  oval(-10,8,4,6,'#f9f1dc');oval(10,8,4,6,'#f9f1dc');oval(0,4,10,12,'#dfcba6');oval(-11,-3,5,9,'#70513c');oval(11,-3,5,9,'#70513c');oval(0,-1,11,10,'#f9f1dc');oval(0,5,4,3,'#302932');oval(-4,0,1,1,'#302932');oval(4,0,1,1,'#302932');
  oval(0,-9,15,4,'#8261a6');ctx.beginPath();ctx.moveTo(-10,-10);ctx.lineTo(2,-24);ctx.lineTo(9,-10);ctx.closePath();ctx.fillStyle='#8261a6';ctx.fill();ctx.stroke();ctx.fillStyle='#ffe4a2';ctx.fillRect(-2,-15,4,3);ctx.restore();
  stats.textContent=`${elapsed.toFixed(1)}s · ${state.moves} steps`;
 }
 function step(dx,dy){if(moveMaze(state,dx,dy)){started=true;fullMap=false;dirty=true;dialog.querySelector('[data-action="overview"]').textContent='Full map';if(state.won){finishMinigame('maze'+state.cells,rewardRun);rewardRun=null;recordMinigame('maze'+state.cells,{time:elapsed,steps:state.moves,seed:state.seed});held=null;result.textContent=`You escaped! ${elapsed.toFixed(1)} seconds · ${state.moves} steps. Try another labyrinth.`;}draw();}}
 function clear(){held=null;nextStep=0;}
 function reset(same=false){clear();state=makeMaze(same?state.seed:crypto.getRandomValues(new Uint32Array(1))[0],Number(select.value));elapsed=0;started=false;dialog.classList.toggle('maze-large',state.cells===86);rewardRun=state.cells===86?null:beginMinigame('maze'+state.cells);fullMap=false;zoom=2;result.textContent='';for(const el of dialog.querySelectorAll('.maze-view-controls,.maze-overview,.maze-map-note'))el.hidden=state.cells!==86;dialog.querySelector('[data-action="overview"]').textContent='Full map';buildBackdrop();draw();}
 function tick(time){if(!dialog.open)return;const dt=lastTime?(time-lastTime)/1000:0;lastTime=time;if(!document.hidden&&document.hasFocus()){if(started&&!state.won)elapsed+=dt;if(held&&time>=nextStep){step(...held);nextStep=time+110;}}if(time-lastStats>100){stats.textContent=`${elapsed.toFixed(1)}s · ${state.moves} steps`;lastStats=time;}draw();frame=requestAnimationFrame(tick);}
 function near(){return !blocked()&&Math.hypot(player.position.x-site.x,player.position.z-site.z)<12&&Math.abs(player.position.y-height(site.x,site.z))<8;}
 function open(){if(!near()||document.querySelector('dialog[open]'))return false;lastFocus=document.activeElement;onStart();reset();dialog.showModal();canvas.focus();enter.hidden=true;lastTime=0;frame=requestAnimationFrame(tick);return true;}
 dialog.addEventListener('close',()=>{cancelAnimationFrame(frame);clear();onEnd();lastFocus?.focus?.({preventScroll:true});});
 dialog.addEventListener('keydown',e=>{const d=dirs[e.code];if(!d||e.target===select)return;e.preventDefault();e.stopPropagation();if(!e.repeat){held=d;step(...d);nextStep=performance.now()+180;}});
 addEventListener('keyup',e=>{if(dirs[e.code])clear();});addEventListener('blur',clear);document.addEventListener('visibilitychange',()=>{clear();lastTime=0;});
 for(const b of dialog.querySelectorAll('[data-dir]')){b.addEventListener('pointerdown',e=>{e.preventDefault();b.setPointerCapture(e.pointerId);held=b.dataset.dir.split(',').map(Number);step(...held);nextStep=performance.now()+180;});for(const event of ['pointerup','pointercancel','lostpointercapture'])b.addEventListener(event,clear);b.addEventListener('click',e=>{if(e.detail===0)step(...b.dataset.dir.split(',').map(Number));});}
 for(const [action,delta]of [['zoom-in',-1],['zoom-out',1]])dialog.querySelector('[data-action="'+action+'"]').onclick=()=>{zoom=Math.max(0,Math.min(zoomLevels.length-1,zoom+delta));fullMap=false;dirty=true;dialog.querySelector('[data-action="overview"]').textContent='Full map';draw();};dialog.querySelector('[data-action="overview"]').onclick=e=>{fullMap=!fullMap;e.target.textContent=fullMap?'Follow wizard':'Full map';dirty=true;draw();};
 select.onchange=()=>reset();dialog.querySelector('[data-action="retry"]').onclick=()=>reset(true);dialog.querySelector('[data-action="new"]').onclick=()=>reset();dialog.querySelector('[data-action="exit"]').onclick=()=>dialog.close();enter.onclick=open;
 return{site,open,near,get state(){return state},get view(){return {fullMap,tiles:zoomLevels[zoom],staticBuilds,viewport:mazeViewport(state,zoomLevels[zoom])}},get active(){return dialog.open},update(time){enter.hidden=!near()||!!document.querySelector('dialog[open]');beacon.rotation.y=time;}};
}
