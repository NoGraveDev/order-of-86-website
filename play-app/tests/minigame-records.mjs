import assert from 'node:assert/strict';
import {createMinigameRecords,MINIGAME_RECORD_KEY,SLED_BEST_KEY,BOAT_BEST_KEY} from '../public/minigame-records.js';
const values=new Map([[SLED_BEST_KEY,'80.5']]),storage={getItem:k=>values.get(k)??null,setItem:(k,v)=>values.set(k,v)};let s=createMinigameRecords(storage);
assert.equal(s.read().records.sled.bestTime,80.5);assert.equal(s.read().records.sled.count,0);
s.finish('sled',{time:95,gates:7,penalty:4,hits:0});s.finish('sled',{time:75,gates:9,penalty:0,hits:0});assert.equal(s.read().records.sled.bestTime,75);assert.equal(s.read().records.sled.count,2);assert.equal(s.read().records.sled.bestGates,9);
for(let i=0;i<8;i++)s.finish('maze9',{time:30+i,steps:60+i,seed:i});s.finish('maze13',{time:100,steps:200,seed:42});s=createMinigameRecords(storage);const a=s.read().records;assert.equal(a.maze9.count,8);assert.equal(a.maze9.bestTime,30);assert.equal(a.maze9.bestSteps,60);assert.equal(a.maze9.recent.length,5);assert.equal(a.maze13.count,1);assert.equal(a.maze13.bestTime,100);assert.equal(a.sled.count,2);
values.set(BOAT_BEST_KEY,'70.5');assert.equal(s.read().records.boat.bestTime,70.5);assert.equal(s.read().records.boat.count,0);s.finish('boat',{time:65,penalty:5});assert.equal(s.read().records.boat.bestTime,65);assert.equal(s.read().records.boat.count,1);assert.equal(createMinigameRecords(storage).read().records.boat.recent.length,1);
assert.throws(()=>s.finish('maze9',{time:NaN,steps:1}));assert.throws(()=>s.finish('sled',{time:1,gates:10,penalty:0,hits:0}));
const blocked=createMinigameRecords({getItem:k=>storage.getItem(k),setItem:()=>{throw Error('quota')}});blocked.finish('maze9',{time:1,steps:5,seed:5});assert.equal(blocked.read().records.maze9.count,9);assert.equal(blocked.read().saved,false);blocked.finish('maze9',{time:2,steps:6,seed:6});assert.equal(blocked.read().records.maze9.count,10);assert.equal(createMinigameRecords(storage).read().records.maze9.count,8);
s.finish('maze86',{time:600,steps:5000,seed:86});s.finish('crossword',{time:120,puzzle:'violet-words-v1'});assert.equal(createMinigameRecords(storage).read().records.maze86.count,1);assert.equal(createMinigameRecords(storage).read().records.crossword.recent[0].puzzle,'violet-words-v1');assert.equal(s.read().records.sled.count,2);
values.set(MINIGAME_RECORD_KEY,'{"sled":{"recent":[{"time":1,"finished":1}]}}');assert.equal(createMinigameRecords(storage).read().records.sled.recent.length,0);values.set(MINIGAME_RECORD_KEY,'broken');assert.equal(createMinigameRecords(storage).read().records.maze9.count,0);
console.log('PASS prior sled best retained, separate difficulties, totals/bests/history, reload, corrupt data, unavailable-storage session fallback');
