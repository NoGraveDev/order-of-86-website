// Extensible prop registry + Blender pipeline.
const directoryGroups=new Map();for(const item of directoryItems){if(!directoryGroups.has(item.category)){const group=document.createElement('optgroup');group.label=item.category;$('objectKind').append(group);directoryGroups.set(item.category,group)}const o=document.createElement('option');o.value=item.kind;o.textContent=item.name;directoryGroups.get(item.category).append(o)}
function selectedItemLore(){const k=$('objectKind').value;return directoryItems.find(d=>d.kind===k)||workshopStories[k]}
$('readItemLore').onclick=()=>{const d=selectedItemLore();if(d){$('loreTag').textContent='PAWTHEON ITEM DIRECTORY';$('loreTitle').textContent=d.name;$('loreText').textContent=d.lore;openDialog('lore')}};
$('objectKind').onchange=()=>{const d=selectedItemLore();$('itemLore').textContent=d?d.lore:'A placeable scene object. Scale, rotate, or duplicate it after placing.';if(d?.color)$('objectColor').value=d.color};
const placed=new T.Group();placed.name='User_Placed_Objects';world.add(placed);let selected=null,serial=0;const records=[];
function containProp(g){containWorldObject(g);g.position.y=height(g.position.x,g.position.z)}
function ahead(){return {x:player.position.x+Math.sin(player.rotation.y)*5,z:player.position.z+Math.cos(player.rotation.y)*5}}
function makeProp(kind,x,z,color){if(directoryKinds.includes(kind))return makeDirectoryItem(kind,x,z,color,{mesh,orb,beam,torus,cube,ico},placed);if(extraItems.includes(kind))return makeExtraItem(kind,x,z,color,{mesh,orb,beam,torus,cube,ico},placed);return tintStoryAsset(placeStoryAsset(kind==='tree'?'oak':kind==='stone'?'rock':kind==='mushroom'?'mushroom':'crystal',x,height(x,z),z,placed),color)}
function refresh(){const sel=$('selection');sel.innerHTML='';for(const g of placed.children){const o=document.createElement('option');o.value=g.uuid;o.textContent=g.name;sel.append(o)}if(selected){sel.value=selected.uuid;$('scale').value=selected.scale.x}}
function addProp(rec){const g=makeProp(rec.kind,rec.x,rec.z,rec.color);g.name=rec.name||(directoryItems.find(d=>d.kind===rec.kind)?.name||rec.kind)+' '+(++serial);g.userData.placement={...rec};if(rec.kind==='brazier'&&rec.lit===true)g.getObjectByName('Brazier_Flame').visible=true;g.scale.setScalar(rec.scale||1);g.rotation.y=rec.rotation||0;containProp(g);selected=g;refresh();achievements.record('builds',rec.kind);return g}
$('addObject').onclick=()=>{const p=ahead();addProp({kind:$('objectKind').value,color:$('objectColor').value,...p,scale:1,rotation:0});$('builder').close();notify('Object placed. Open Build to adjust or export it.')};$('selection').onchange=()=>{selected=placed.children.find(x=>x.uuid===$('selection').value);refresh()};$('scale').oninput=()=>{if(selected){selected.scale.setScalar(+$('scale').value);containProp(selected)}};$('rotateObject').onclick=()=>{if(selected){selected.rotation.y+=Math.PI/4;containProp(selected)}};$('moveObject').onclick=()=>{if(selected){const p=ahead();selected.position.set(p.x,height(p.x,p.z),p.z);containProp(selected);$('builder').close();notify('Object moved ahead')}};$('duplicateObject').onclick=()=>{if(!selected){notify('Select a placed object first.');return}if(!selected.userData.placement){notify('Duplicate is available for workshop objects.');return}const p=ahead();addProp({...selected.userData.placement,name:selected.name+' copy',...p,scale:selected.scale.x,rotation:selected.rotation.y});$('builder').close();notify('Copy placed ahead.')};$('deleteObject').onclick=()=>{if(selected){placed.remove(selected);selected=placed.children.at(-1)||null;refresh()}};
function download(blob,name){const a=document.createElement('a'),url=URL.createObjectURL(blob);a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),5000)}
$('exportLayout').onclick=()=>{const data=placed.children.filter(g=>g.userData.placement).map(g=>({...g.userData.placement,name:g.name,x:g.position.x,z:g.position.z,scale:g.scale.x,rotation:g.rotation.y}));download(new Blob([JSON.stringify({format:'pawtheon-layout-v1',objects:data},null,2)],{type:'application/json'}),'pawtheon-layout.json');notify('Layout saved. Imported GLB objects need a world export.')};
$('importLayout').onchange=async e=>{try{const data=JSON.parse(await e.target.files[0].text());if(data.format!=='pawtheon-layout-v1'||!Array.isArray(data.objects)||data.objects.length>500)throw Error();for(const r of data.objects){if(!['tree','stone','crystal','mushroom',...extraItems,...directoryKinds].includes(r.kind)||!Number.isFinite(r.x)||!Number.isFinite(r.z)||Math.abs(r.x)>WORLD_RADIUS||Math.abs(r.z)>WORLD_RADIUS||!/^#[0-9a-f]{6}$/i.test(r.color)||!Number.isFinite(r.scale)||r.scale<.2||r.scale>6||!Number.isFinite(r.rotation))throw Error()}data.objects.forEach(addProp);notify('Layout added to your world.')}catch{notify('Could not load this layout. Choose a Pawtheon layout JSON.')}e.target.value=''};
const loader=new GLTFLoader();async function loadGLB(file,asPlayer){if(!file)return;try{if(file.size>80*1024*1024)throw Error('Choose a GLB smaller than 80 MB.');const gltf=await loader.parseAsync(await file.arrayBuffer(),'');const model=gltf.scene;let box=new T.Box3().setFromObject(model),size=box.getSize(new T.Vector3());const s=(asPlayer?2.8:4)/Math.max(size.x,size.y,size.z);model.scale.setScalar(s);box.setFromObject(model);let center=box.getCenter(new T.Vector3());model.position.set(-center.x,-box.min.y,-center.z);const wrapper=new T.Group();wrapper.add(model);wrapper.name=file.name.replace(/\.glb$/i,'');if(asPlayer){activeStarter=null;try{gameStorage.removeItem(STARTER_SAVE_KEY);}catch{}mixer?.stopAllAction();customAvatar=true;$('playerName').textContent=wrapper.name;$('playerTraits').textContent='IMPORTED CHARACTER';player.remove(avatar);avatar=wrapper;player.add(avatar);mixer=gltf.animations.length?new T.AnimationMixer(model):null;actions={};for(const clip of gltf.animations)actions[clip.name.toLowerCase()]=mixer.clipAction(clip);lastAction='';}else{const p=ahead();wrapper.position.set(p.x,height(p.x,p.z),p.z);placed.add(wrapper);containProp(wrapper);selected=wrapper;refresh()}$('builder').close();notify(asPlayer?'Your character is ready to walk.':'Blender object added. Export the world to keep it.')}catch(e){notify(e.message||'Unable to import. Use an embedded, uncompressed GLB file.')}}
$('importObject').onchange=e=>{loadGLB(e.target.files[0],false);e.target.value=''};$('importPlayer').onchange=e=>{loadGLB(e.target.files[0],true);e.target.value=''};
$('exportWorld').onclick=async()=>{const b=$('exportWorld');b.disabled=true;b.textContent='Preparing export…';try{await Promise.all(realms.slice(1).map(ensureLandmark));const exportRoot=new T.Group();const exportedWorld=world.clone(true);const exportedCrowd=exportedWorld.getObjectByName('The_Order_of_86_Roaming_Dogs');if(exportedCrowd)for(const actor of exportedCrowd.children)actor.visible=!actor.name.startsWith('NPC_'+activeDog.id+'_');exportRoot.add(exportedWorld);const p=player.clone(true);exportRoot.add(p);const exportMaterials=new Map();exportRoot.traverse(o=>{if(o.isMesh&&o.material?.isMeshToonMaterial){const old=o.material;if(!exportMaterials.has(old))exportMaterials.set(old,new T.MeshStandardMaterial({color:old.color,vertexColors:old.vertexColors,map:old.map,roughness:1,side:old.side}));o.material=exportMaterials.get(old)}});await new (await import('./GLTFExporter.js')).GLTFExporter().parseAsync(exportRoot,{binary:true,onlyVisible:true}).then(data=>download(new Blob([data],{type:'model/gltf-binary'}),'pawtheon-world.glb'));notify('World exported. In Blender: File → Import → glTF 2.0.')}catch(e){notify('Export failed: '+e.message)}finally{b.disabled=false;b.textContent='Export world .glb'}};
